<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.ProductMapper">
    
    <resultMap id="BaseResultMap" type="Product">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="category" property="category"/>
        <result column="image" property="image"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM product WHERE status = 1 ORDER BY create_time DESC
    </select>

    <select id="findByCategory" resultMap="BaseResultMap">
        SELECT * FROM product WHERE category = #{category} AND status = 1 ORDER BY create_time DESC
    </select>

    <select id="searchByName" resultMap="BaseResultMap">
        SELECT * FROM product 
        WHERE name LIKE CONCAT('%', #{keyword}, '%') AND status = 1 
        ORDER BY create_time DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, description, price, stock, category, image, status)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{category}, #{image}, #{status})
    </insert>

    <update id="update" parameterType="Product">
        UPDATE product
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="category != null">category = #{category},</if>
            <if test="image != null">image = #{image},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM product WHERE id = #{id}
    </delete>
</mapper>





